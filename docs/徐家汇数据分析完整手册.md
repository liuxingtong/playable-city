# 徐家汇数据分析完整手册

> 整合 CLD 因果回路、平台数据与衍生指标、回路子系统、SD 仿真边函数、**CSVI 脆弱性模型**与系统内可视化，统一命名与逻辑。  
> 版本：v5.0 · MVR 仿真 · 2026 年 3 月（增补 §5.8 系统 CSVI 模型页与三维度地图；手册更名）

---

## 一、CLD 回路简介

### 1.1 研究背景

徐家汇适老化改造采用**人机共生城市系统**建模，通过 CLD（因果回路图）描述老年人聚集、街道舒适、认知恢复、代际互动等变量之间的因果关系，并识别人机装置部署的优先路段。

### 1.2 核心回路类型

| 类型 | 含义 | 示例 |
|------|------|------|
| **R 强化回路** | 正反馈，变量相互增强 | R1 社交活力、R4 认知恢复 |
| **R 注入链** | 外生变量注入，驱动下游 | R2 步行可达、R3 环境舒适 |
| **R 恶性回路** | 负向正反馈，系统恶化 | R_bad 交通诱导（车多→噪声→不愿步行→更多人开车） |
| **B 平衡回路** | 负反馈，系统自调节 | B2 拥挤调节、B3 认知超载 |
| **B 抑制回路** | 外生抑制，削弱目标 | B5 文化摩擦、B6 隔代锁定 |

### 1.3 关键枢纽节点

- **N02**：老年人聚集强度，多数回路的交汇点
- **N18**：人机共生干预指数，设计变量（仿真时拨动 0/0.6）
- **N09**：街道舒适度，MVR 版为内生变量，闭合 B2 拥挤调节回路
- **N10**：车流量代理，MVR 版为内生变量，闭合 R_bad 交通诱导回路

### 1.4 人机干预机制

人机干预**不改变 CLD 拓扑**，只改变边的传递函数参数。干预开启时：

| 边 | 修改内容 |
|----|----------|
| N_YP→N_IG | 虚线(权重≈0) → 实线(激活) |
| N_GC→N16 | 负向抑制系数减弱 |
| N14→N15 | 倒U型峰值右移（系统能承受更高认知复杂度） |
| N03→N14 | 正向传递增益提高 |

6–9 个月出现相变点，N_IG 达临界密度后自我强化，R4 与行为回路产生协同效应。

---

## 二、平台数据与衍生指标清单

### 2.1 数据源总览

| 数据源 | 存储路径 | 空间形态 | 坐标系 |
|--------|----------|----------|--------|
| 街景分割 | `streetview_images/*.csv` | 点 (lon, lat) | WGS84 |
| 人口栅格 | `population/*.tif` | 100m 栅格 | WGS84 |
| POI | `poi_data/*.csv` | 点 (lon, lat) | GCJ→WGS84 |
| 房价小区 | `house_data/*.xlsx` | 点 (lon, lat) | WGS84 |
| 用地 | `landuse_data/*.geojson` | 多边形 | WGS84 |
| 路网 | `road/*.graphml` | 边 + 节点 | WGS84 |

### 2.2 街景衍生指标

| 指标 | 含义 | 计算方式 |
|------|------|----------|
| GVI | 绿视率 | vegetation + terrain |
| SVF | 天空可见度 | sky |
| Shade_Comfort | 遮蔽舒适度 | (vegetation + building + bridge + tunnel - sky) 归一化 |
| Motor_Pressure | 机动车压力 | car + bus + truck + motorcycle + road |
| Shannon_H | Shannon 多样性 | -Σ(pᵢ·ln(pᵢ))，按 7 大语义类别 |
| Complexity | 视觉复杂度 | 非零类别数 × Shannon_H，归一化 |
| Ped_Friendly | ART 兼容 | sidewalk + bench + pedestrian area + street light - barrier - fence |
| ART_Score | ART 综合恢复潜力 | 0.3×Fascination + 0.25×Being_Away + 0.2×Coherence + 0.25×Ped_Friendly |

### 2.3 人口、POI、用地、路网

- **人口**：`pop_65plus`、`pop_15_59`、`pop_0_14` 栅格采样
- **POI**：边中点 100m 半径内 `poi_social`、`poi_medical`、`poi_culture`、`poi_total`
- **用地**：网格 `shannon`、`green_rate`、`n_types`
- **路网**：`highway_type`、`length`、`maxspeed`、`lanes`，`edge_traffic_pressure` = motor_flag × (lanes/4) × (maxspeed/60) 归一化

### 2.4 CLD 变量与数据映射

**说明**：路网边计算（`analysis/road/cld.py`）使用 N08 表示街道舒适度；SD 仿真与 CLD 模型统一使用 N09。两者为同一概念，公式均为 GVI + Shade_Comfort + Ped_Friendly（0.4:0.3:0.3）。

| CLD 节点 | 含义 | 平台数据/指标 | 所属回路 |
|----------|------|---------------|----------|
| N01 | 社交场所密度 | poi_social + poi_culture + 0.8×poi_medical | R1 |
| N02 | 老年人聚集强度 | pop_65plus 归一化 | R1, R5, B2 |
| N03 | 街道界面活跃度 | Shannon_H + Complexity + poi_total | R1 |
| N04 | 路网连通性 | α/β/γ、介数中心性 | R2 |
| N05 | 步行可达性半径 | 等时圈 500m 内 65+ 人口 | R2 |
| N06 | 土地混合度 | landuse_shannon | R2 |
| N07 | 绿化遮阴覆盖率 | 0.6×GVI + 0.4×Shade_Comfort | R3 |
| N08/N09 | 街道舒适度（边表列名）；另见 **5.7 注** | 0.4×GVI + 0.3×Shade + 0.3×Ped_Friendly（边计算用 N08，SD 用 N09） | R3, B2 |
| N10 | 车流量代理 | edge_traffic_pressure | R_bad |
| N11 | 噪声与污染代理 | 同 N10，叠加 Motor_Pressure | R_bad |
| N14 | 空间认知复杂度 | 0.4×Complexity + 0.35×landuse_shannon + 0.25×poi_total | R4, B3 |
| N15 | 认知储备激活潜力 | ART_Score | R4 |
| N17 | 环境生理压力 | 0.5×Motor_Pressure + 0.5×edge_traffic_pressure | B3 |
| N_MED | 医疗 POI 可达性 | medical_isochrone 或 medical_poi_density | medical |
| N_WAIT | 候诊溢出停留 | 1 - medical_waiting_quality | medical |
| N_YP | 年轻人白天在场密度 | pop_15_59 | 外生 |
| N_GC | 隔代照料强度 | pop_65plus × pop_0_14 | 外生 |

**仿真内生节点**（无直接数据）：N13 步行意愿、N16 主动探索意愿、N18 人机干预、N19 行为数据密度、N20 干预节点优化。

---

## 三、回路子系统

### 3.1 子系统分解图

```
┌─────────────────┐         ┌─────────────────┐
│  Sub-A 行为社会  │ ──N02──→│  Sub-B 认知恢复  │
│  N02, N09, N10  │ ──N09──→│  N15, N16        │
│  R1/R2/R3/      │         │  R4/B3/B6        │
│  R_bad/B2/B5    │         └────────▲─────────┘
└────────┬────────┘                  │
         │                           │ N_IG→N15
         │         ┌─────────────────┴─────────┐
         └────────→│  Sub-C 代际激活            │
                   │  N_IG                     │
                   │  R6                       │
                   └───────────────────────────┘
```

| 子系统 | 名称 | 节点 | 回路 |
|--------|------|------|------|
| Sub-A | 行为社会回路 | N02, N09, N10 | R1, R2, R3, R_bad, B2, B5 |
| Sub-B | 认知恢复回路 | N15, N16 | R4, B3, B6 |
| Sub-C | 代际激活 | N_IG | R6 |

### 3.2 外部回路一览

| 回路 | 名称 | 类型 | 外生输入 |
|------|------|------|----------|
| R1 | 社交活力 | R 强化 | N01, N03 |
| R2 | 步行可达 | R 注入链 | N04, N05, N06 |
| R3 | 环境舒适 | R 注入链 | N07 |
| R_bad | 交通诱导 | R 恶性 | N_YP |
| B2 | 拥挤调节 | B 平衡 | —（MVR：N09 内生闭合） |
| B5 | 文化摩擦 | B 抑制 | N_YP |
| R4 | 认知恢复 | R 强化 | N14 |
| B3 | 认知超载 | B 平衡 | N14, N17 |
| B6 | 隔代锁定 | B 抑制 | N_GC |
| R6 | 代际激活 | R 注入 | N_YP |
| medical | 就医行为链 | R 注入 | N_MED |

---

## 四、SD 仿真边函数清单（MVR 版）

### 4.1 变量角色

| 角色 | 变量 | 说明 |
|------|------|------|
| **内生** | N02, N09, N10, N15, N16, N_IG, N19, N20 | 8 个，由方程更新 |
| **外生** | N01, N03, N05, N07, N14, N17_base, N_YP, N_GC, N10_base | 固定输入 |

### 4.2 控制参数

| 参数 | 默认值 | 含义 |
|------|--------|------|
| N18 | 0.0 | 人机干预强度（0=无，0.6=干预） |
| alpha | 2.0 | N18 对 N_YP→N_IG 的放大系数 |
| beta | 0.15 | N_IG→N15 注入强度 |
| gamma | 0.6 | N18 对 N10 的削弱系数 |
| delta | 0.5 | N18 对 N17 的削弱系数 |

### 4.3 一步更新方程（离散化）

**N10（车流量）**：
```
N10_eq = clip(N10_base + k10_NYP·N_YP, 0, 1) × (1 - gamma·N18)
dN10 = 0.30 × (N10_eq - N10)
```

**N09（街道舒适度）**：
```
N09_eq = clip(N09_base + k09_N07·N07 - k09_N02·N02 - k09_NYP·N_YP - 0.10·N10, 0, 1)
dN09 = 0.20 × (N09_eq - N09)
```

**N_IG（代际互动）**：
```
N_IG_target = clip(N_YP × (1 + alpha·N18), 0, 1)
dN_IG = tau_IG × (N_IG_target - N_IG)
```

**N17 有效值**：
```
N17_eff = clip(N17_base × (1 - delta·N18) + 0.15·N10, 0, 1)
```

**N15（认知储备）**：
```
dN15 = k14·N14 - k17·N17_eff·N15 + beta·N_IG - k15_decay·N15
```

**N16（探索意愿）**：
```
dN16 = k16_N15·N15 - k16_GC·N_GC - k16_decay·N16
```

**N02（聚集强度）**：
```
N02_eq = clip(k02_social·N01·N03 + k02_access·N05 + k02_comfort·N09, 0, 1)
dN02 = 0.15×(N02_eq - N02) - k02_crowd·N02²
```

**N19, N20（R5 人机共生，仅 N18>0）**：
```
dN19 = k19_N02·N02·N18 - k_b4·N18·N19 - k19_decay·N19
dN20 = k20_N19·N19 - k20_decay·N20
```

### 4.4 模型参数默认值

| 参数 | 值 | 含义 |
|------|-----|------|
| k09_N07 | 0.15 | 绿化→舒适 |
| k09_N02 | 0.20 | 拥挤→舒适（负，B2） |
| k09_NYP | 0.15 | 工作人口→舒适（负，B5） |
| k09_decay | 0.10 | N09 自然衰减 |
| N09_base | 0.5 | 舒适度基线 |
| k10_NYP | 0.25 | 工作人口→车流 |
| k10_decay | 0.15 | N10 自然衰减 |
| k02_social | 0.10 | (N01·N03)→N02 |
| k02_access | 0.08 | N05→N02 |
| k02_comfort | 0.12 | N09→N02 |
| k02_crowd | 0.05 | N02² 拥挤自限 |
| k14 | 0.10 | N14→N15 |
| k17 | 0.25 | N17 对 N15 抑制 |
| k15_decay | 0.15 | N15 衰减 |
| k16_N15 | 0.15 | N15→N16 |
| k16_GC | 0.15 | N_GC→N16（负） |
| k16_decay | 0.10 | N16 衰减 |
| tau_IG | 0.10 | N_IG 收敛速率 |

### 4.5 边传递函数汇总表（与 CLD 对应）

| 起点 | 终点 | 极性 | 函数形式 | 回路 |
|------|------|------|----------|------|
| N01 | N02 | + | k02_social·N01·N03 | R1 |
| N03 | N02 | + | 同上 | R1 |
| N02 | N03 | + | 闭环 | R1 |
| N05 | N02 | + | k02_access·N05 | R2 |
| N07 | N09 | + | k09_N07·N07 | R3 |
| N09 | N02 | + | k02_comfort·N09 | R3 |
| N02 | N09 | - | -k09_N02·N02 | B2 |
| N_YP | N09 | - | -k09_NYP·N_YP | B5 |
| N_YP | N10 | + | k10_NYP·N_YP | R_bad |
| N10 | N09 | - | -0.10·N10 | R_bad |
| N_GC | N02 | + | 线性 | R6 |
| N_YP | N_IG | + | N_YP·(1+α·N18) | R6 |
| N_IG | N15 | + | β·N_IG | R6 |
| N14 | N15 | + | k14·N14（线性近似，完整版为倒U型） | R4 |
| N17 | N15 | - | -k17·N17·N15 | B3 |
| N15 | N16 | + | k16_N15·N15 | R4 |
| N_GC | N16 | - | -k16_GC·N_GC | B6 |

**注**：medical 链（N_MED、N_WAIT）在 cld_subsystems_and_edges 中定义，MVR 仿真器暂未实现，路网边计算已支持挂载。

---

## 五、改造优先级计算（CSVI 模型）

采用 **CSVI（认知-社会脆弱性指数）** 计算边级改造优先级，CSVI 越高越优先改造。

### 5.1 公式

```
CSVI_i = (E_i × S_i) / (AC_i + 1)
```

- **E**（暴露度）：高认知风险老年人在此路段驻留的加权密度
- **S**（敏感性）：环境认知伤害 + 代际接触浪费
- **AC**（适应能力）：物理缓冲 × 社会缓冲

分母接近零时 CSVI 趋向极大，表示该路段几乎无设计缓冲，优先级最高。

### 5.2 暴露度 E

五类老年人画像的加权在场密度，权重反映认知风险：

| 画像 | 权重 | 在场密度 nₖ |
|------|------|-------------|
| 独居高龄老人 | 1.0 | pop_65plus × (1 + poi_social_norm + poi_market_norm) |
| 医疗重度使用者 | 0.9 | pop_65plus × (1 + poi_medical_norm) |
| 隔代照料者 | 0.6 | pop_65plus × (1 + poi_sport_norm) |
| 晨练社群组织者 | 0.5 | pop_65plus |
| 活跃新老年（60–64岁） | 0.4 | pop_60_64 |

`E_i = normalize(Σ wₖ × nₖᵢ)`，结果归一化至 [0,1]。

### 5.3 敏感性 S

两种伤害叠加：`S_i = S_env_i + S_contact_i`

- **S_env**（环境认知伤害）：`Motor_Pressure × (1 - Complexity)`，车流噪声激活生理应激，视觉复杂度可部分对冲  
  - 数据来源：街景分割 → `Motor_Pressure`（car+bus+truck+motorcycle+road）、`Complexity`（非零类别数×Shannon_H）
- **S_contact**（代际接触浪费）：`pop_15_59_norm × (1 - Q)`，其中 `Q = Ped_Safety × Ped_Friendly`  
  - 数据来源：街景 `Ped_Safety`、`Ped_Friendly`；人口 `pop_15_59`

### 5.4 适应能力 AC

`AC_i = AC_phys_i × AC_social_i`

- **AC_phys**：`Ped_Infra × N09 × alpha`，步行设施 × 街道舒适度 × 路网 α 连通指数  
  - 数据来源：街景 `Ped_Infra`（sidewalk+crosswalk+curb cut+pedestrian area+lane marking）、N09（GVI+Shade+Ped_Friendly）
- **AC_social**：`Ped_Friendly × (poi_social + poi_culture) / max(...)`  
  - 数据来源：街景 `Ped_Friendly`；POI 分类 `poi_social`、`poi_culture`

**AC 归一化与中心化**：原始 AC_phys、AC_social 先 min-max 归一化至 [0,1]，再对 AC_combined = AC_phys_norm × AC_social_norm 做二次归一化并中心化（减均值），用于 CSVI 分母。

**说明**：若 `merged.csv` 为原始分割数据（无 Motor_Pressure、Ped_Infra 等列），流水线会自动调用 `streetview_osm_visualization.compute_all_indicators` 计算衍生指标后再挂载。

分母保护：`CSVI = E × S ÷ (AC+1)`，AC+1 避免除零且更稳健。

### 5.5 计算流程与进度

流水线步骤（含 CSVI 子步骤）：

1. 加载路网  
2. 挂载街景指标（含 Ped_Infra、Ped_Safety）  
3. 挂载 POI 分类（含 poi_sport、poi_market）  
4. 挂载用地网格  
5. 挂载人口栅格（含 pop_60_64）  
6. 计算车流量代理  
7. 计算 CLD 变量  
8. 计算暴露度 E  
9. 计算敏感性 S  
10. 计算适应能力 AC  
11. 计算 CSVI 综合得分  
12. 写入边属性  
13. 导出结果  

输出同时包含 `csvi_E`、`csvi_S_env`、`csvi_S_contact`、`csvi_AC_phys`、`csvi_AC_social`，便于诊断每条路段得分高的原因。

### 5.6 预计算脚本

运行 `python scripts/precompute_cld_priority.py` 可提前执行完整流水线（含 E、S、AC、CSVI），结果保存至 `cache/cld_priority.pkl`。仪表盘启动时若存在该缓存，将直接加载，无需在界面内重新计算。

建议在首次使用或数据更新后运行一次，以加速仪表盘加载。

### 5.7 停留意愿矩阵与街段三类节点（可视化实现口径 · 2026-03）

以下与叙事页 `pages/1narrative-framework.html`、矩阵 `src/matrix/csvi_quadrant.jsx` / `dist/src/matrix/stay_willingness_matrix.html`、大地图 `map_intervention_nodes.html` 及卡片 `node-type-cards-v3.html` **当前实现**对齐，便于与 CLD/CSVI 主公式对照讲解。

#### 停留意愿矩阵（四象限）

| 符号 | 实现公式 | 说明 |
|------|----------|------|
| **W_elder** | **E × (AC_phys + 1)** | 纵轴；在 AC_phys 近 0 时仍保留 E 的基底，避免整条轴塌缩 |
| **W_work** | **N_YP × N08** | 横轴；叙事中的 **Q** 与 CSV 列 **N08** 为同一乘子（0–1 量级归一化标量） |
| **象限划分** | 十字线为各轴 **原始 W** 的 **中位数**（四象限）；与「三档高低」粒度不同 | 与 `csvi_quadrant.jsx` 一致 |
| **W 轴三档（节点地图）** | 先算研究区内 **经验分位** P_elder、P_work∈[0,1]（平均秩处理并列），再对 **P** 做 Jenks 自然断点三档 | 「高档」= 该轴上**相对位置**靠前，两轴可比；`map_intervention_nodes.html`、`xujiahui-site-selection.html`（及独立页 `field_system_selection.html`）与矩阵数据层一致 |

**注**：敏感性叙事中 **S_contact ∝ pop_15_59 × (1 − Q)** 的 **Q** 与矩阵横轴 **Q（N08）** 为同一套符号；若上游对 N08 的构权与「Ped_Safety×Ped_Friendly」不完全一致，以 **`cld_priority.csv` 表头及生成脚本**为准。

#### 大地图 `map_intervention_nodes.html` 与矩阵的 W 轴（已对齐）

- **W_elder** = **E × (AC_phys + 1)**
- **W_work** = **N_YP × N08**（**Q ≡ N08**，与 `csvi_quadrant.jsx`、场域选址页 `xujiahui-site-selection.html` / `field_system_selection.html` 一致）
- **可比性**：对 **W_elder、W_work** 各自在**当前边表**上算经验分位 **P_elder、P_work**，**激活 / 临界**等规则里对 W 的 Jenks 三档在 **P** 上计算（非在原始 W 上），使「老年轴高档」与「工作轴高档」表示**同一相对强度**。

历史版本曾用 **N_YP − S_contact** 作地图横轴简化式，已弃用；更早曾直接对原始 W 做 Jenks，两轴量纲不可比。

#### 节点地图判定 · 潜力与资源（`map_intervention_nodes.html`，与 `node-type-cards-v3` 叙事对齐）

| 类型 | 条件摘要 |
|------|----------|
| **潜力 · 激活** | `priority`（CSVI）Jenks **最高档**；且 **W_elder 或 W_work** 至少其一为 Jenks **最高档**。子类：仅老年轴高 / 仅工作轴高 / 双高 |
| **资源** | 本条 **未** 判为激活；**聚合 AC**（四主导列均值，缺列时用 `csvi_AC_phys`）在研究区内 Jenks **最高档**（不与潜力重复打点） |

**地图绘制（与分类独立）**：潜力（**仅**激活三亚型）与资源的 **Leaflet 标记**先按边段中点是否落在 **大徐家汇四街道并集**（`data/daxujiahui-four-streets-union.geojson`，与 `xujiahui-osm-boundary.js` 玫红边界一致）过滤，再在**边界内子集**中按主指标降序取 **前 10%**（潜力：`priority`；资源：聚合 **AC_agg**；每类至少 1 条边）。底图路网为 **大徐家汇边界内** 边段 **全量** 绘制（边段中点在界内即参与；`priority` 着色在边界内子集上 min–max 归一化，**不再**按全表 priority 抽样）。**本页不加载** `catalyst-seam-clusters.js` / `corridor-bottleneck-routing.js`，**不展示**瓶颈/廊道图层。（**场域选址页** `xujiahui-site-selection.html` / `field_system_selection.html` 仍保留 **临界** 分类与触媒 DBSCAN 并集逻辑，与本图独立；两页在分类与廊道计算**之前**用 **`filterEdgeRecordsToDaxujiahuiStudy`** 将输入边段限制为**中点落在同一研究区多边形内**，与节点地图一致。）

#### 瓶颈与廊道 · 衔接带 + 路径层（`catalyst-seam-clusters.js` + `corridor-bottleneck-routing.js`）

**叙事—几何对齐**：触媒 **DBSCAN 簇**与接缝仍由 **`catalyst-seam-clusters.js`** 生成；**瓶颈打分 b(e)** 与 **簇间廊道**由 **`corridor-bottleneck-routing.js`** 的 `CorridorBottleneck.compute` 统一计算。**合并场域选址页**（`xujiahui-site-selection.html`，叙事 S9 嵌入）**默认不绘**廊道折线；**独立页**（`field_system_selection.html`）以**玫红折线**绘簇间路径。**干预节点地图**（`map_intervention_nodes.html`）**不再**参与瓶颈/廊道计算与标注（已改为潜力+资源逻辑，见上表）。

**A. 衔接边（接缝）**（与 2026-03 版一致）  

1. **触媒 DBSCAN**：候选 = **激活 ∪ 临界**，抽样规则与场域页相同（25% / 至少 20 / cap 200；ε 自 180 m 等）。  
2. **激活顶点赋簇种**（候选边优先 priority、否则最近邻中点簇）；**无权多源 BFS** 染全图。  
3. **衔接边集** = 两端顶点簇号不同之路网边。  
4. **退化**：种子不足时回退 **k-means k=2** / **经度对半** 后再 BFS。

**B. 边级改造吸引度 b(e)∈[0,1]**（**凸组合**，与廊道权函数共用）  

- **nPri**：全表 `priority` min-max 归一。  
- **接缝示性**：边 ∈ 衔接边集则为 1，否则 0。  
- **脆弱 AC**：若 `csvi_AC_phys ≤` 全域下中位则为 1；否则按中位→**95 分位**线性降至 0。  
- **b(e) = 0.45·nPri + 0.35·接缝 + 0.2·脆弱AC**（截断至 [0,1]）。

**C. 瓶颈（点/边层）**  

- **瓶颈边**（实现）：若 **廊道边集非空**，取 **廊道上的边** ∩ **AC_phys ≤ 全域下中位** ∩ **b(e) ≥ τ**；否则 **衔接边** ∩ 同两条件（默认 **τ = 0.42**，`BOTTLENECK_B_MIN`）。  
- **独立场域页** `field_system_selection.html` 在无簇间路径数据时，图绘仍可回退「全接缝 / 低 AC」（`getEdgesToDrawAsBottleneck`）；**合并页**不执行该图绘分支。**分类**始终按上式输出的瓶颈边列表。

**D. 廊道（路径层，簇间一条可走路径）**  

1. **簇**：与场域相同的 DBSCAN 非噪声簇（≥2 点）。  
2. **接口 T_k**：每簇取 **几何 medoid 顶点**——在簇内触媒边端点集合中，选到**各触媒边中点** haversine 距离和最小的顶点（避免「整街多源」漂移）。  
3. **簇对**：在路网上算簇间 **BFS 跳数**；先做 **互为最近邻** 无向对；若簇图不连通，则反复加入 **跨分量最小跳数边**，且单边跳数 ≤ **260**（`BRIDGE_MAX_HOPS`）。  
4. **加权最短路**：对每一簇对，在**全边表无向图**上 **Dijkstra**，边权 **w = 0.55·l̂ + 0.45·(1−b(e))**，其中 **l̂** = 边长 / 全图最长边；按簇对 **跳数升序** 依次求路，已出现过的边叠加 **占用惩罚**（系数 **γ=0.35**），减轻多廊道完全重叠。  
5. **输出**：**`field_system_selection.html`** 用 **玫红实线折线**（Leaflet）绘路径；**`xujiahui-site-selection.html`** 不绘该层（合并页统计区可显示路径条数等）。**`map_intervention_nodes.html`** 不使用该路径层。

**依赖**：`corridor-bottleneck-routing.js` 须在 **`catalyst-seam-clusters.js` 之后**加载。

### 5.8 系统内 CSVI 模型页与三维度地图（实现清单）

本仓库叙事页与地图中的 **CSVI** 与 §5.1–§5.5 **同一套流水线产物**；示意图侧重可读性，**分项构权与归一化细节**以 `cld_priority.csv` **表头及生成脚本**为准。

#### 结构页 `csvi-model.html`（Cognitive–Social Vulnerability Index）

| 区块 | 内容 |
|------|------|
| 主方程带 | **E × S ÷ (AC+1) = CSVI**（与 §5.1 一致；**+1** 为分母稳健项） |
| 示意展开 | 页面底部小字 **\[Σwₖnₖ\] × \[S_env + S_contact\] ÷ \[AC_phys × AC_social\]**，对应 §5.2–§5.4 的分项形态；**不单独再写 +1**，避免版式重复 |
| **E** | 五类老年人画像权重条 + Σ(wₖ·nₖ) 聚合示意 |
| **S** | **S_env**：Motor_Pressure × (1 − Complexity)；**S_contact**：工作人口密度 × (1 − Q) |
| **AC** | **AC_phys**：Ped_Infra × N09 × α；**AC_social**：Ped_Friendly × 社交/文化 POI（示意，完整项见 §5.4） |
| 底部三卡 | **E↑** / **S↑** / **AC↓** 的空间可读解释 |

**嵌入位置**：叙事页 `1narrative-framework.html` 第 **S5** 节，`iframe data-src="csvi-model.html"`。

#### 三维度空间地图（Leaflet，数据源 `cld_priority.csv`）

| 页面 | 维度标签 | 地图使用的列 / 派生量 |
|------|----------|------------------------|
| `map_E_exposure.html` | CSVI Dimension **01** · 暴露 | `csvi_E`（玫红圆点深浅 = 相对最大值的归一化透明度） |
| `map_S_stressor.html` | CSVI Dimension **02** · 敏感 | `csvi_S_env`、`csvi_S_contact`；第三档为 **S_total = S_env + S_contact**（图例文案 `csvi_S_total`） |
| `map_AC_buffer.html` | CSVI Dimension **03** · 适应 | `csvi_AC_phys`、`csvi_AC_social`；第三档为 **AC_total = AC_phys × AC_social**（图例文案 `csvi_AC_total`） |

上述页面与 `map_intervention_nodes.html`、场域选址页 **`xujiahui-site-selection.html`**（叙事 **S9** iframe；合并轴线/选址/数据层）及独立页 **`field_system_selection.html`** 共用研究区边界数据 **`data/daxujiahui-four-streets-union.geojson`**（合并页内联 `fetch` 绘制玫红虚线样式；亦可用可选脚本 **`xujiahui-osm-boundary.js`** 在其它地图页叠加）；**须通过 HTTP 打开**（如 `npm run dev`），以便 `fetch` 本地 CSV / GeoJSON。按大徐家汇筛边表：`npm run filter:cld-daxujiahui`。  
**触媒簇、接缝、瓶颈 b(e)、廊道路径**依赖 **`cluster-field-circles.js`**（须在 **`catalyst-seam-clusters.js`** 与 **`corridor-bottleneck-routing.js`** 之前）、后两者同上。  
**场域边界**：**`xujiahui-site-selection.html`** 与 **`field_system_selection.html`** 均为每块场域绘制 **实线核心圆**与 **虚线缓冲圆**（较大者为缓冲）：**r<sub>叙事</sub>=√(该块分配面积/π)**，**r<sub>点云</sub>** = 触媒中点质心至最远中点 + 缓冲；**r<sub>内</sub>=min**，**r<sub>外</sub>=max**。**medoid** 在 **DBSCAN 簇**上用同一套 min/max 得到 **r<sub>外</sub>** 约束路口；若圆内无任何簇内路口，则 **逐步放大半径**直至有候选或上限，仍无则退回全顶点（避免断图）。路径中段仍走公共路网，**仅接口顶点**保证落在外圆内（放宽后）。

#### 边表列与综合得分

| 列名 | 含义 |
|------|------|
| **`priority`** | 边级改造优先级 **综合得分，即 CSVI**；§5.7 中 Jenks 分档、节点地图「潜力·激活」分档以其为主轴 |
| `csvi_E` | 暴露度 E |
| `csvi_S_env` | 敏感性 · 环境认知伤害分项 |
| `csvi_S_contact` | 敏感性 · 代际接触浪费分项 |
| `csvi_AC_phys` | 适应能力 · 物理缓冲分项（表列；若同存四主导列且该行四格均有值，则 **`map_AC_buffer.html`、停留意愿矩阵**用四列算术平均替代本列参与 AC_phys 与 W_elder） |
| `csvi_AC_social` | 适应能力 · 社会缓冲分项 |
| `AC_med_dom` / `AC_tech_dom` / `AC_mkt_dom` / `AC_sport_dom` | 医疗资源主导 / 科技 / 商业 / 体育（可选）；**四值在同一行均非空**时参与上述聚合；**`map_intervention_nodes.html`** 资源点另取四列 **argmax** 为子类型 |

主数据文件：`data/cld/cld_priority.csv`；构建部署可同步至 `public/cld_priority.csv`（见 `scripts/sync-cld.mjs`）。

#### 相关页面速查

| 页面 | 作用 |
|------|------|
| `dist/src/matrix/stay_willingness_matrix.html` / `src/matrix/csvi_quadrant.jsx` | 停留意愿矩阵（W_elder / W_work），§5.7 |
| `node-type-cards-v3.html` | 潜力 / 资源 / 廊道（卡片示意；地图页不绘廊道） |
| `catalyst-seam-clusters.js` | 触媒 DBSCAN 与接缝（多源 BFS） |
| `corridor-bottleneck-routing.js` | b(e)、严格瓶颈筛选、簇 medoid 间加权廊道（`xujiahui-site-selection.html` + `field_system_selection.html`；**不含** `map_intervention_nodes.html`） |
| `cluster-field-circles.js` | DBSCAN 簇 medoid 用圆约束（质心 + 半径）；与场域页展示圆（10 ha 比例）独立计算 |
| `cld_2d_interactive.html` | 2D CLD 交互（节点数据口径：**§2.4、§4.1**） |

---

*整合自：平台数据与衍生指标清单、cld_subsystems_and_edges、cld_intervention_mechanism、sd_simulator_mvr.py、CSVI_model_spec、implementation_checklist、**csvi-model.html**、**map_E_exposure / map_S_stressor / map_AC_buffer**、map_intervention_nodes、**xujiahui-site-selection.html** / **field_system_selection.html**、csvi_quadrant 实现 · 2026 年 3 月*
