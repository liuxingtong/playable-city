# Agent 踏勘 × 外部案例库（索引）

RAG 命令行、`trace_id` / `agent_id`、SSE **`retrieval`**、`since_event_id`、**30s** 高亮、`GET /events` 回放及异常隔离的完整说明见：

→ **[`docs/agent-recon-contracts.md` §6 外部案例库](./agent-recon-contracts.md#case-base-integration)**

一键启动（含静态站 + RAG 代理；可选事件子进程）：`npm run start:agent-recon`（契约文档 §2）。

本文件保留为历史链接锚点。
